### HOW to RUN this Project
First, clone the project, open cmd in the project directory, then run these commands:
```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
py manage.py runserver
```
